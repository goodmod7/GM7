{
  "name": "ai-operator",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "pnpm -w lint:ci",
    "lint:ci": "pnpm --filter @ai-operator/web lint && pnpm --filter @ai-operator/api typecheck && pnpm --filter @ai-operator/shared typecheck && pnpm --filter @ai-operator/desktop typecheck",
    "render:api:build": "pnpm --filter @ai-operator/shared build && pnpm --filter @ai-operator/api prisma:generate && pnpm --filter @ai-operator/api build",
    "render:api:start": "pnpm --filter @ai-operator/api start",
    "render:api:migrate": "pnpm --filter @ai-operator/api prisma:generate && pnpm --filter @ai-operator/api migrate:deploy",
    "smoke:from-zero": "bash scripts/smoke/httpSmoke.sh",
    "smoke:ws": "bash scripts/smoke/wsSmoke.sh",
    "smoke:final": "bash scripts/final-smoke.sh",
    "test": "pnpm --filter @ai-operator/shared test && pnpm --filter @ai-operator/api test && node --import tsx --test --test-force-exit tests/**/*.test.mjs tests/**/*.test.ts",
    "typecheck": "turbo run typecheck",
    "version:check": "node scripts/version-check.mjs",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "check:desktop:csp": "node scripts/check-desktop-csp.mjs",
    "check:desktop:security": "node scripts/check-desktop-security.mjs",
    "release:verify:github": "node scripts/release/verify-github-release.mjs",
    "release:verify:api": "node scripts/release/verify-api-feed.mjs",
    "release:verify": "pnpm release:verify:github && pnpm release:verify:api"
  },
  "devDependencies": {
    "@types/node": "^20",
    "prettier": "^3.3.0",
    "tsx": "^4.19.0",
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  }
}
